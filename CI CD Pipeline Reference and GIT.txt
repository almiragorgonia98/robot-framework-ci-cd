1. Create Repository in GitHub
2. Perform GIT command 
git add . 
git commit -m "Adding comments"

git switch main or New-Branch
git switch -c New-Branch
git push or git push -u origin main or New-Branch

:wq 
Enter

To merge from Branch to main or vice versa
git switch main
git pull origin main
git merge New-Branch
git push origin main

To copy a text files from Branch to main or vice versa 
git fetch origin
git restore --source origin/ci-cd -- "Test.txt"
git add "Test.txt"
git commit -m "Copy Test.txt from ci-cd"
git push origin main

Shortcut Keys to open Developer Tools
Ctrl + Shift + I

To see the list of commit
git log --oneline

To compile the commit
git rebase -i HEAD~3 (number of commits from the last row)
pick
squash (change)
squash (change)



